Drop a .zip ROM and an image (png/jpg/jpeg/webp/gif/avif) into each game folder.
Filenames are free — the scanner picks the first .zip and the first image it finds.
Folder layout: public/games/<platform>/<slug>/<anything>.zip + <anything>.png